[package]
name = "p2159-rs"
version = "0.1.0"
edition = "2021"

[dependencies]
sea-orm = { version = "1.1", features = ["sqlx-sqlite", "runtime-tokio-native-tls"] }
futures = "0.3.7"
tokio = { version = "1", features = ["full"] }  # tokio 的全功能支持

